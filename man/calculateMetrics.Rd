% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateMetrics.r
\name{calculateMetrics}
\alias{calculateMetrics}
\title{Calculate behavioural diversity metrics from BORIS observations}
\usage{
calculateMetrics(
  x,
  zoo = c("Zoo", "Beekse", "Safaripark", "Tiergarten"),
  age = c("Mládě", "Zyqarri"),
  plotMetrics = FALSE,
  whichPlot = "ShannonH",
  cols = c("#3C8ABF", "#A1BCD7", "#768D1A", "#B1BE94")
)
}
\arguments{
\item{x}{A data frame created by \code{mergeBoris()}, containing columns \code{animal}, \code{Behavior},
\code{file}, and \code{cumDuration}.}

\item{zoo}{Character vector of text patterns used to recognise zoo files
(default includes \code{"Zoo"}, \code{"Beekse"}, \code{"Safaripark"}, \code{"Tiergarten"}).}

\item{age}{Character vector of text patterns identifying juveniles in the \code{animal} column
(default includes \code{"Mládě"}, \code{"Zyqarri"}).}

\item{plotMetrics}{Logical. If \code{TRUE}, plots boxplots of the selected metrics
by group.}

\item{whichPlot}{Character vector naming one or more metrics to display.
Partial matching is allowed. Available metrics are:
\code{"ShannonH"}, \code{"HillD2"}, \code{"PielouEvenness"},
\code{"transitionsPerHour"}, and \code{"medianBout"}.}

\item{cols}{Character vector of four colours used for plotting,
corresponding to zoo adult, zoo juvenile, wild adult, and wild juvenile.}
}
\value{
A data frame where each row corresponds to one animal and columns include:
\code{animal}, \code{zoo}, \code{adult}, \code{file}, and the computed metrics described above.
If \code{plotMetrics = TRUE}, boxplots of selected metrics are drawn.
}
\description{
This function summarises each individual's behaviour using a set of diversity
and temporal metrics, allowing comparison between elephants observed in zoos
and in the wild, and between adults and juveniles.
}
\details{
It computes indices describing both how many different behaviours were
expressed and how evenly the animal distributed its time among them,
as well as the \emph{temporal structure} of its activity.
Optionally, it can visualise the metrics as grouped boxplots.

The function removes the \code{"Mimo dohled"} category and computes, for each animal:
\itemize{
\item \strong{timeOnCamera} - Total duration (s) during which the animal was visible and scored.
\item \strong{ShannonH} – Shannon diversity index.
Quantifies overall behavioural diversity, combining both richness (number of behaviours)
and evenness (balance in time spent on each behaviour).
Higher values indicate a broader and more evenly distributed behavioural repertoire.
\item \strong{HillD2} - Simpson reciprocal diversity (Hill number of order 2).
Expressed as the \emph{effective number of equally common behaviours}.
A value of 5 means the activity distribution is as diverse as if five behaviours
were expressed equally often.
\item \strong{PielouEvenness} - Pielou’s evenness index.
Measures how equally the animal divides its time among all behaviours.
Values near 1 denote balanced activity; low values indicate dominance of few behaviours.
\item \strong{transitionsPerHour} - Frequency of behavioural switches.
The number of changes between behaviours per hour of observation.
Reflects temporal flexibility: higher rates imply frequent switching between activities.
\item \strong{medianBout} - Median duration (s) of individual behaviour bouts.
Represents the typical length of a continuous behaviour.
Shorter bouts indicate more dynamic behaviour; longer bouts suggest more persistent activity.
}

When \code{plotMetrics = TRUE}, the function produces grouped boxplots comparing the
four categories: zoo adults, zoo juveniles, wild adults, and wild juveniles.
These plots help visualise whether, for example, wild elephants show greater
behavioural diversity or evenness than zoo animals, or whether juveniles
switch behaviours more frequently than adults.
}
\examples{
\dontrun{
dat <- read.table("Data/elephantBehaviour.txt", header = TRUE, sep = "\t")
res <- calculateMetrics(dat,
  plotMetrics = TRUE,
  whichPlot = c("Shannon", "Hill")
)
head(res)
}

}
