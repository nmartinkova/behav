% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPCA.r
\name{plotPCA}
\alias{plotPCA}
\title{PCA of behavioural composition}
\usage{
plotPCA(
  x,
  zoo = c("Zoo", "Beekse", "Safaripark", "Tiergarten"),
  age = c("Mládě", "Zyqarri"),
  cols = c("#3C8ABF", "#A1BCD7", "#768D1A", "#B1BE94")
)
}
\arguments{
\item{x}{A data frame created by \code{mergeBoris()}, containing columns \code{animal}, \code{Behavior},
\code{file}, and \code{cumDuration}.}

\item{zoo}{Character vector of text patterns used to recognise zoo files
(default includes \code{"Zoo"}, \code{"Beekse"}, \code{"Safaripark"}, \code{"Tiergarten"}).}

\item{age}{Character vector of text patterns identifying juveniles in the \code{animal} column
(default includes \code{"Mládě"}, \code{"Zyqarri"}).}

\item{cols}{Character vector of four colours used for the groups in order:
ZOO adult, ZOO juvenile, Wild adult, Wild juvenile.
The default palette uses blue for zoo animals and green for wild animals, with lighter
tones representing juveniles.}
}
\value{
Invisibly returns a list containing:
\itemize{
\item \code{pca} - the PCA result (\code{prcomp} object)
\item \code{data} - the Hellinger-transformed behavioural proportion matrix
}
}
\description{
Performs a Principal Components Analysis (PCA) on the relative time
each elephant spends performing different behaviours, using a
Hellinger transformation for proportional data.
The function produces a multiframe figure that visualises individual
behavioural profiles in PCA space and the contribution of each behaviour
to the retained principal components.
}
\details{
Each individual's behaviour durations are summed and converted to
relative proportions of total visible time.
The Hellinger transformation (\code{sqrt(p)}) is applied to reduce the influence
of dominant behaviours and make Euclidean distances more appropriate
for proportional data.

The analysis identifies the main gradients in behavioural composition.

The number of principal components shown in the loading heatmap is
estimated using the \emph{broken-stick} criterion, which retains components
explaining more variance than expected under random partitioning.

The function creates three panels:
\enumerate{
\item PCA scatter plot of individuals (PC1 vs PC2).
\item Heatmap showing absolute loadings of each behaviour on the retained PCs.
\item Colour legend for loading magnitudes.
}

\strong{Note:} If you receive the error
\code{"Error in plot.new(): figure margins too large"},
enlarge the plotting window or output device (e.g., use \code{pdf(..., width = 10, height = 12)}
or a larger image size) before running the function.
}
\examples{
\dontrun{
dat <- read.table("Data/elephantBehaviour.txt", header = TRUE, sep = "\t")
res <- plotPCA(dat)
# To view variable loadings, use:
res$pca
# To view the Hellinger-transformed behavioural proportion matrix, use:
res$data
}

}
